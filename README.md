# Smart-CSV-Data-Analyzer---CS50-Project
Smart CSV Data Analyzer is a Python command-line tool that reads CSV files, cleans data, calculates basic statistics, generates histogram charts, and creates an automatic text report.
